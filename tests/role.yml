---
- hosts: localhost
  remote_user: root

  roles:

# by forcing one pass and one failure, we can confirm pass and fail working

    - { role: testForCommandResponse, command: "nano --version", expected: "GNU nano", debug: true, immediate_exit_on_fail: false }

    - { role: testForCommandResponse, command: "nano --version", expected: "GNU Nano", debug: true, immediate_exit_on_fail: false }




